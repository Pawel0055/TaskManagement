{% if is_granted("ROLE_USER") %}
    {% for task in tasks %}
        {{ task.title }}
    {% endfor %}

Dodawanie nowego taska:
    {{ form_start(task_form) }}
        {{ form(task_form) }}
    {{ form_end(task_form) }}
{% endif %}

